<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_layout"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:popupTheme="@style/AppTheme.PopupOverlay">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:id="@+id/textView"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:text="Medical List"
                android:textColor="#FFF"
                android:textSize="24sp" />

            <ImageView
                android:id="@+id/iv_medical_list_add"
                android:layout_width="@dimen/imageview_toolbar_width"
                android:layout_height="@dimen/imageview_toolbar_width"
                android:layout_centerVertical="true"
                android:layout_toLeftOf="@+id/iv_medical_list_search"
                android:layout_toStartOf="@+id/iv_medical_list_search"
                android:src="@drawable/ic_add" />

            <ImageView
                android:id="@+id/iv_medical_list_search"
                android:layout_width="@dimen/imageview_toolbar_width"
                android:layout_height="@dimen/imageview_toolbar_width"
                android:layout_alignParentRight="true"
                android:layout_centerVertical="true"
                android:layout_margin="@dimen/imageview_toolbar_margin"
                android:src="@drawable/ic_search" />

        </RelativeLayout>
    </android.support.v7.widget.Toolbar>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_medical_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>