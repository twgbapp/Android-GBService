<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.TextView" parent="TextAppearance.AppCompat.Large">
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="AppTheme.RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/main_light_gray</item>
        <item name="colorControlActivated">@color/main_light_yellow</item>
    </style>

    <style name="expend_list_item">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="RadioButton.LightBlue" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="colorAccent">@color/main_light_blue</item>
        <item name="colorControlHighlight">@color/main_light_blue</item>
        <item name="android:colorPressedHighlight">@color/main_light_blue</item>
        <item name="colorPrimaryDark">@color/main_light_blue</item>
        <item name="colorPrimary">@color/main_light_blue</item>
        <item name="colorControlActivated">@color/main_light_blue</item>
    </style>
</resources>
